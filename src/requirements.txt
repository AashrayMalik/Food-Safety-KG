# Food Safety KG — project dependencies (grouped by pipeline stage).
# Pinned versions reflect the last-known working environment.  Packages
# without a pin should be matched to your local torch/transformers build.

# ---- Core pipeline (extraction + validation LLM calls, canonicalisation, RDF) ----
httpx==0.28.1
networkx>=3.0
rapidfuzz>=3.0
rdflib>=6.3
requests==2.32.5

# ---- Corpus building (PDF -> markdown, web scraping) ----
beautifulsoup4>=4.12
markitdown==0.1.6
selenium==4.44.0

# ---- Canonicalisation / splitting / NLI evaluation ----
numpy==2.4.6
pandas==3.0.3
openpyxl==3.1.5
scikit-learn==1.9.0
scikit-multilearn==0.2.0

# ---- NLI validation (cross-encoder) ----
sentence-transformers

# ---- Local HuggingFace extraction backend ----
transformers==5.12.1
torch
